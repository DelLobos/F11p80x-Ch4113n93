// Swap src/assets/logo/logo.png for a different file if you have a
// different logo asset - the surrounding AppHeader layout won't need to
// change.
import logoSrc from '../../assets/logo/logo.png';

export default {
  name: 'AppLogo',
  data() {
    return { logoSrc };
  },
};
